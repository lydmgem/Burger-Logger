<h1>Mangé le burger!</h1>

<h2>Bon appétit!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{burger_name}}
      {{> burgers/burger-block}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Ordres Précédentes</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
      {{id}}
      {{burger_name}}
      </li>
    {{/if}}
  {{/each}}
</ul>

<h2>L'ordre</h2>
<form class="create-form">

  <div class="form-group">
    <label for="newBurger">Burger Name:</label>
    <input type="text" id="newBurger" name="burger">
  </div>

  <button type="submit">Order!</button>
</form>
